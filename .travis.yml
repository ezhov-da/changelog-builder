language: java

jdk:
  - openjdk8

# https://stackoverflow.com/questions/33820638/travis-yml-gradlew-permission-denied
before_install:
  - cat ./install-dependencies.sh
  - chmod +x gradlew

#install:
#

script:
  - mvn cobertura:cobertura -B
  - ./gradlew build

after_success:
  - bash <(curl -s https://codecov.io/bash)